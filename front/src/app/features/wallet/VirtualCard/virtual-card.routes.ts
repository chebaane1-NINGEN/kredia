import { Routes } from '@angular/router';
import { VirtualCardPageComponent } from './pages/virtual-card-page/virtual-card-page.component';

export const routes: Routes = [
  { path: '', component: VirtualCardPageComponent }
];
