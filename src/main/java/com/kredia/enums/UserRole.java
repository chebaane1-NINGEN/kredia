package com.kredia.enums;

/**
 * @deprecated Use {@link com.kredia.common.Role} instead.
 *             This enum is kept for backward compatibility with entity mappings
 *             but should not be used in new code.
 */
@Deprecated
public enum UserRole {
    ADMIN,
    CLIENT,
    AGENT
}